# agent-framework-benchmark
What We're Building? The same "Company Research Agent" workflow implemented in **5 frameworks**, with automated benchmarking and LLM-as-judge evaluation. This benchmark is **local-first** — it runs on local models via Ollama by default, with cloud providers (OpenAI, Anthropic) as configurable alternatives.
